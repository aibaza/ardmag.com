# Vercel — copiaza in Project Settings > Environment Variables
# Valorile cu <...> trebuie completate

MEDUSA_BACKEND_URL=https://api.ardmag.ro
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=<pk_... din Medusa admin>

NEXT_PUBLIC_BASE_URL=https://ardmag.ro
NEXT_PUBLIC_DEFAULT_REGION=ro

NEXT_PUBLIC_STRIPE_KEY=<pk_live_...>

REVALIDATE_SECRET=<genereaza cu: openssl rand -hex 32>

# R2 CDN hostname pentru image optimization
NEXT_PUBLIC_R2_HOSTNAME=media.ardmag.ro
NEXT_PUBLIC_BACKEND_HOSTNAME=api.ardmag.ro

# SMTP2GO (pentru contact form si emailuri storefront)
# Varianta 1 — API key (preferata)
SMTP2GO_API_KEY=<api_key_din_smtp2go_dashboard>
# Varianta 2 — SMTP relay
SMTP_HOST=mail.smtp2go.com
SMTP_PORT=587
SMTP_USERNAME=<smtp username>
SMTP_PASSWORD=<parola>
# Adrese sender @ardmag.ro
SMTP_FROM_CONTACT=contact@ardmag.ro
SMTP_FROM_NOREPLY=no-reply@ardmag.ro
SMTP_REPLY_TO=office@ardmag.ro
CONTACT_NOTIFY_EMAIL=office@ardmag.ro

# GA4 + Meta Pixel (dupa lansare)
NEXT_PUBLIC_GA4_MEASUREMENT_ID=<G-XXXXXXXX>
NEXT_PUBLIC_META_PIXEL_ID=<pixel_id>
